hey claude, i need to merge all the html files in this directory into one html for a single web page. If you check the files, you'll see they include tab      like buttons at the bottom, i want those to work as tabs/buttons that allow to navigate the content. At the moment each html file is one page or view of the    overal thing, which is educational material. So instead of having a long page with all the content, i want to use the tabs at the bottom to navigate. Can     you please do a first try in a new html file where you aim to achieve this? The approach: each tab's content lives in a hidden <div>, and each tab's CSS is  
  loaded dynamically on switch so the color themes (orange/yellow/white/green/amber/blue/cyan/magenta) stay fully isolated.
